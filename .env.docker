# Database Configuration
POSTGRES_USER=taiga
POSTGRES_PASSWORD=changeme_secure_password
POSTGRES_DB=taiga_db

# Taiga API Configuration
TAIGA_BASE_URL=https://taiga.vuce.gob.ar/
# NOTA: El token NO se configura aquí, se proporciona desde la interfaz web
# Dejar vacío o comentado:
# TAIGA_AUTH_TOKEN=
TAIGA_TOKEN_TTL=82800

# Application Settings
LOG_LEVEL=info

# Grafana Configuration
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# IMPORTANTE:
# 1. Copy this file to .env.docker
# 2. El token de Taiga se configura desde la interfaz web en http://localhost:8001/table-map
#    (hay un modal que solicita el token al abrir la página)
# 3. También puedes configurarlo via API: POST http://localhost:8001/auth {"token": "tu_token"}
# 4. Change POSTGRES_PASSWORD and GRAFANA_ADMIN_PASSWORD for production
